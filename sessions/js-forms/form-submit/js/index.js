console.clear();

/*
Exercise: Form submit

- Add an event listener for the submit event of the form.
- Get the data of the different input elements, put it in an object and push it to the 'data' array.
- After every submit, reset the form so all the inputs are empty again.

Important!: Make sure you cancel the default browser behavior of the submit event
 */

const data = [];
