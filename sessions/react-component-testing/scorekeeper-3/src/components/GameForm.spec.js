import {render, screen} from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import GameForm from './GameForm';

describe('GameForm', () => {
	it.todo('renders two input fields and a button');

	it.todo('renders a form with the name "Create a new game"');

	it.todo('submits form data when every field is filled out');

	it.todo('does not submit form if one input field is left empty');
});
