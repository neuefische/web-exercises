{
  "name": "template-js",
  "version": "0.0.0-unreleased",
  "description": "JavaScript (with Jest support)",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "npx -y serve@latest",
    "lint": "npx -y eslint@latest .",
    "test": "NODE_OPTIONS=--experimental-vm-modules npx -y jest@latest --watchAll"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "eslint-plugin-jest": "^27.2.2",
    "eslint": "^8.43.0"
  },
  "nf": {
    "template": "js"
  },
  "private": true
}
